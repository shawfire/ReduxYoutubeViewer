// Create a new compoent.
//  This component should produce some HTML.
const App = function() {
  return <div>Hi!</div>;  // JSX - What looks like HTML in our JavaScript
}
// Take this component's generated HTML and put it on the page (in the DOM)
